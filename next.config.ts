import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['picsum.photos', 'images.unsplash.com', 'cdn.pixabay.com', 'i.imgur.com'],
  },
  /* config options here */
};

export default nextConfig;
